let ws;

function connectWebSocket()
{
    ws = new WebSocket('ws://loaclhost:3006');
    ws.onopen = () => {console.log('conected to WS server')};
    ws.onmessage = () => {};
    ws.onclose = () =>{};
}

function sendMessage()
{
    if (ws.readState === WebSocket.OPEN)
    {
        ws.sendMessage('hey in mivta');
    }
    
}

connectWebSocket();